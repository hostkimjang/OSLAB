# Optional SupplyScan-specific values for docs/supplyscan_docs scenarios.
# Copy these into config/oslab.local.env only when running SupplyScan private smoke tests.
OSLAB_SUPPLYSCAN_SERVER_URL=https://supplyscan.example.local
OSLAB_SUPPLYSCAN_SABUN=10001

# Optional deterministic asset name. When omitted, oslab uses oslab-{ScenarioId}-{VmId}.
# OSLAB_ASSET_NAME=oslab-win11-agent-smoke
